# U-A1.1 fix mandate (Fable, per dual FAIL)
Read .orchestration/reports/U-A1-review-opus.md and U-A1-review-sonnet.md. Ordered fixes:
1. (Opus F1 = Sonnet S1, BLOCKING) arm_command honours a rising Arm edge ONLY in {Disarmed, LatchedSafe}; clear pending_arm_edge on every fault transition (and on Disarm). Regression tests: arm pressed during WARNING and during ESCALATED → after ACK→LatchedSafe + dwell_rearm + good frames, state stays LatchedSafe and authorised stays false until a FRESH post-latch arm. Extend the randomized invariant test: generator must include acknowledge() and arm-during-alarm events, and add the invariant "authorised can only become true following an arm edge accepted in Disarmed/LatchedSafe since the last revocation."
2. (Sonnet S2, HIGH) Enforce simultaneous-event priority in the LIVE supervisor: make event application state-aware/merged so fault-beats-disarm holds regardless of call order within a tick (wire simultaneous_successor in, or delete it and implement the equivalent guard logic — state which). Tests drive BOTH orderings (disarm-then-fault-solution, fault-solution-then-disarm) through the live supervisor and assert identical outcome incl. alarm annunciation.
3. (Sonnet S3, MED) DR-fill frames must reach the supervisor: the executive's IMU-propagate path emits a propagated solution epoch through integrity.solution() (and the NDJSON seam) so §1.1's lease semantics are real during LEO outage. Choose and justify the cadence approach (every propagate tick, or decimated toward the 5 Hz nominal with cadence control [UNVERIFIED]); tests: all-LEO-loss scenario where DR frames renew the lease while G2 holds and expire authority via t_dr when the absolute-observation age exceeds it (proving t_dr, not t_lease, governs).
4. (Opus F2) now_ns monotonicity guard: a backward step is detected, treated as a G-false fault (revoke path) + debug_assert; tests with backward and repeated timestamps.
5. (Opus F3) Add acknowledge to the gate trait + an Ack helm input: CONTRACTS v5 amendment (v5.1 append — do not rewrite v5) defining the Ack bus message sibling to ArmCommand; executive routes it to the supervisor; routing test.
6. (Opus F4) Couple params+validator: the only way to construct a grant-capable supervisor requires BOTH complete params AND an explicit calibration validator; params-complete-with-default-validator must be unrepresentable or a hard error; test.
7. Correct the report's clause table rows flagged as overstating coverage (Sonnet rows 2/6/7/8) — cite the NEW tests.
Gate: PATH="$HOME/.cargo/bin:$PATH" cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check. TDD: red tests first (commit), then fixes.
