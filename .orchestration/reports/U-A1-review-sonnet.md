(Reviewer: Sonnet, fresh context, second seat. Verdict: FAIL. Independently reproduced Opus F1.)
- S1 CRITICAL/high = Opus F1 (same bug, independent repro): arm during WARNING → pending_arm_edge survives latch → re-grant without fresh post-latch arm. Shipped latch test never presses arm in W/E; randomized invariant test never calls acknowledge() so LatchedSafe is unreachable, and its invariant (!authorised || (g2&&g3)) cannot encode the fresh-arm rule by construction.
- S2 HIGH/high lib.rs:164 — simultaneous_successor is DEAD CODE: never called by the live supervisor; priority tests call the free function directly. Disarm-then-fault ordering silently absorbs the fault (no alarm), inverting the documented fault-beats-disarm guarantee. Report's clause-table rows citing it are misleading.
- S3 MED/med — executive emit_epoch only fires on GNSS/Doppler update paths; IMU-propagate ticks never call integrity.solution(), so DR-propagated fill NEVER renews/exercises the lease — during all-LEO loss the lease dies on t_lease regardless of DR quality, contradicting §1.1's "every valid 5 Hz frame including DR-propagated fill." Pre-existing emission behavior but the brief required realizing §1.1.
- Report-accuracy: rows 2/6/7/8 overstate coverage (details in session transcript). Gate reproduces 48/48; diff hygiene clean; determinism clean; matrix table itself verified correct cell-for-cell by independent transcription.
Process note: reviewer found an uncommitted probe test left in the shared worktree by the parallel deep-seat reviewer, traced it, independently rewrote and confirmed the failure, reverted the file, and disclosed the sequence. Coordinator ruling: correct handling; future concurrent reviews of one worktree must not leave probe artifacts (or must run in separate clones).
