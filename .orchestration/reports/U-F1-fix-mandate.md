# U-F1.1 fix mandate (Fable, per dual FAIL)
Execute EVERY item from .orchestration/reports/U-F1-review-opus.md (H1, H2, M1-M5, L1-L5, I2, I3) and .orchestration/reports/U-F1-review-sonnet.md (1-5). Where they overlap (H1≈S4, M3≈S5) one fix serves both — implement a shared ECEF→ENU/NED rotation utility at the estimate's location and use it for: horizontal/vertical accuracies (DRMS over ENU E,N incl. cross-cov; U-sigma vertical), horizontal_velocity_ned_mps, and the speed model. Contracts v3: append the accuracy-formula convention (DRMS), the clock-bias observability decision (M5 — choose document+bound or remove; justify), and the GNSS gating semantics (I3). Every new/changed update path gets an FD Jacobian test THROUGH THE REAL CODE PATH on an AUGMENTED filter (receiver slot + ≥2 nuisances, retire-middle covered), and Estimator::update() dispatcher gets an integration test per payload type. H2: covariance-growth test must fail when Q=0 (verify by temporarily zeroing Q — show it fails — then restore; state this red-run in the report). TDD: separate test commit(s) before implementation commits this round.
