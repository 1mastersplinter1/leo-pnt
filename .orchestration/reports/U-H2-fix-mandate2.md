# U-H2.2 fix mandate (per U-H2.1 dual-review FAIL, review at .orchestration/reports/U-H2.1-review-opus.md)
1. (C1) RE-ENABLE the production chi-square gate in the study (chi_square_threshold = Some(9.0), the SAFETY_CASE production value) — NEVER disable a safety-critical gate to make a study run. Re-run; the real EKF must stay bounded.
2. (C2) HONESTY: report the real bounded result truthfully — with a single-satellite (ISS-TLE) fixture the denied position error is ~tens of km, NOT the 100-200 m class; state plainly WHY (single-sat range-rate geometry is near-unobservable for position; the 100-200 m class needs multi-satellite geometry). Fix error_class() so it never collapses an impossible/huge error into ">=500 m" — add classes up to and beyond Earth-radius and a DIVERGED flag if error exceeds a sane bound. No in-divergence "reconvergence" metrics.
3. (C3) DISCLOSE and reconcile with U-P1 in STUDY.md: U-P1's boundedness came from a clamped toy estimator; this study uses the real EKF; cross-reference D51.
4. (F2) real manoeuvre: make the turn an actual convergence-reset stressor (sharp, realistic rate), and only report reconvergence when the filter is actually converged (not diverged).
5. (F3) the against-main test must use fingerprints ACTUALLY captured from main (git worktree main, generate small(42), capture real FNV) OR assert exact byte-equality against a freshly-generated main artifact in the test; the current hardcoded "captured from main" values are U-H2's own output — FALSE provenance, fix it or the -0.0 z-component delta must be eliminated so bit-identity genuinely holds.
6. (L1) results.json trailing newline so it is byte-reproducible; (L2) verify aided steady-state (covariance-delta threshold) before recording the loss covariance.
7. ADD (new requirement surfaced by C1): a note that a MULTI-SATELLITE fixture study is the required next step to demonstrate the 100-200 m denied class — route as an open item.
Gate: cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check. No attribution trailers. Consistent with D50 (does not claim 20/30 kn denied supported) and D51.
