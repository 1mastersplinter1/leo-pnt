# Adversarial Review — Unit U-H2 (high-speed mission + 20 kn passage study)\n\nWorktree `/home/od/work/leo-pnt-wt-UH2`, commit `7976eb7` vs base `2bb7079`. Gate reproduced green; study reproduced bit-identically. Base worktree created for diffing and removed; U-H2 tree left clean.\n\n## Gate / reproduction (all confirmed)\n- `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets -- -D warnings`: clean. `cargo test`: all pass (pnt-mission `high_speed_tests` 3/3, pnt-studies `highspeed::tests` 3/3, full workspace green).\n- `highspeed-study` regenerates `docs/studies/highspeed/results.json` byte-identically. Headline numbers in the report and STUDY.md match `results.json` exactly (7 kn 43.87/59.47/60.95 m; 20 kn 98.49/134.59/138.01 m; endurance 132.43/181.61/186.27 m; margins −8.57 / +16.50 / +6.00 h). Arithmetic (500 km at 20 kn = 13.50 h; 24 h = 888.96 km) is correct, and the brief's \"20 kn/24 h/~500 km\" inconsistency is honestly flagged and split into two cases.\n\n## Findings\n\n**F1 — HIGH / high confidence — the study never runs the mission generator or the EKF.** `crates/pnt-studies/src/highspeed.rs:225-231,276`. `simulate()` computes every position/velocity/convergence number from a closed-form polynomial with hardcoded constants (`4.0 + 0.75*q.sqrt()*age.sqrt()*speed_factor.sqrt() + 0.000_012*age*speed_factor + phase`; `velocity_at`; `convergence_time = 75.0 + 28.0*speed_factor*q.sqrt()`). The module imports only `pnt_estimator::ProcessNoise` (a config struct) — not the filter, not `pnt_mission`. Grep for `generate_mission|Estimator|filter|integrate|pnt_mission` in the highspeed module/bin returns nothing. Consequence: the three headline mission-generator features the brief ordered (wave/slam, speed-scaled IMU, coordinated turns) are **never exercised end-to-end** — the study reports its own modeling assumptions (speed → larger error is baked into `speed_factor`/`q`, not discovered) and cannot falsify anything. `wave_model` in the report is a descriptive **string**, not an input to any computation. Verify: read `simulate()`; confirm no simulation import.\n\n**F2 — HIGH / high confidence — convergence is not measured, and the \"several legs / three turns\" route is fictional.** `highspeed.rs:291-297`: three `Convergence` rows produced by `(1..=3).map(...)` with the turn index unused, so all three are identical (80.60 s / 0.290 km at 7 kn; 114.19 s / 1.175 km at 20 kn). `distance_m = convergence_time * speed_mps` is a tautology, not an independent distance-to-reconverge measurement. Meanwhile the mission generator (`lib.rs` `heading_profile`) produces exactly **one** 90° turn (window 45–55% of duration); the study's \"four equal-distance legs with 90° turns\" and 3-turn table do not correspond to anything the generator does. The brief's \"convergence after each manoeuvre in TIME AND DISTANCE\" is therefore not satisfied as a measurement (the time-vs-distance-at-20 kn point is illustrated numerically but via formula).\n\n**F3 — MEDIUM / high confidence — backward-compat regression for missions with duration < 10 s; the bit-identical claim is not actually proven.** `crates/pnt-mission/src/lib.rs` `heading_profile` else-branch (post-turn) was refactored from returning `FRAC_PI_2` to `rate*(turn_end-turn_start)` where `rate = FRAC_PI_2/(turn_end-turn_start).max(1.0)`. For turn-window ≥ 1 s (duration ≥ 10 s) this is algebraically identical; for duration < 10 s it yields `FRAC_PI_2*(duration*0.1)` ≠ `FRAC_PI_2`. Empirically verified by regenerating seed 1 from base vs U-H2: **default 180 s mission bit-identical** (manifest + measurements + truth), but **duration 4 s DIFFERS** in both `measurements-000000.seg` and `truth-000000.seg`. The test fixtures (`tests/mission.rs` `small()`) use `duration_s = 4`, so the fixture missions are *not* bit-identical to main. The only determinism test compares new-vs-new (`same_seed_produces_bit_identical_run_directory`), so it structurally cannot detect a regression against main — this is exactly why you asked for a base diff. The report's \"Existing bit-identical directory tests pass\" is true but does not substantiate backward compatibility. Realistic (≥10 s) missions are safe; short missions are not.\n\n**F4 — MEDIUM / high confidence — wave/slam is non-zero-mean (rectified), not a physically sane oscillatory disturbance.** `lib.rs:625` `vertical = peak * (PI*phase).sin()` with `phase ∈ [0,1)` → `sin` is non-negative over the whole burst, so every vertical burst is purely upward and every pitch-coupled horizontal burst is same-signed. Acceleration is bounded (`|slam[2]| ≤ peak` — the \"bounded\" claim holds and is tested), but the burst has a **non-zero mean**: net velocity impulse per burst = `peak*duration*2/π > 0`, accumulating monotonically. A real wave/slam is ~zero-mean; integrating this injects an unbounded DC velocity drift. Seeded/rate-configurable/sample-rate-independent: yes. Pitch-coupling is a static scalar (`slam[0]=vertical*pitch_coupling`), perfectly correlated — a generous but [UNVERIFIED]-labeled stand-in. The rectified/non-zero-mean property is the physically load-bearing caveat and is **not disclosed** in STUDY.md (\"bounded half-sine\" only).\n\n**F5 — MEDIUM / high confidence — IMU/truth dynamic inconsistency (the U-E2 standard).** `lib.rs:239-247`. Truth position/velocity are purely kinematic (`velocity_ne` from heading/speed; truth `velocity_ned` z-component hardcoded 0). The slam is added to the **IMU** acceleration (`slam[2]` on ECEF axis 0; `slam[0]` folded into the NE→ECEF accel) but never to truth. Integrating the generated accelerations therefore does **not** recover the truth velocity profile — it recovers truth + accumulated (rectified) slam + bias + noise. This violates the \"integrate accels → recover velocity including disturbances\" consistency standard: the disturbance exists only in the sensor, not in the body it purports to describe. The brief framed slam as planing *dynamics* (real motion) but it is implemented as a sensor-only error. This does **not** corrupt the committed study numbers — only because F1 means the study never feeds the mission to an estimator — but it is a latent trap: any real EKF consumer (or a future D47 scenario that actually runs the pipeline) will DR-drift on motion the truth never experienced. This is the \"wave model silently invalidates a study\" failure mode; here it is dormant rather than triggered.\n\n**F6 — MEDIUM / high confidence — speed-scaled IMU feature has zero behavioral test coverage.** `SpeedScaledImuConfig` is never constructed as `Some` anywhere in the tree, including tests (grep confirms only the struct def, the field, the `None` default, and the two consuming functions). The scaling branch in `scaled_imu()` (`lib.rs:574-585`) and the corresponding `validate_config` branch (`lib.rs:559`) are exercised only on the `None` path. One of the three headline mission features ships untested for its actual behavior. (Wave and coordinated-turn each have exactly one unit test; adequate-minimal, not thorough — the wave test only checks a single phase point at probability=1 and never exercises the Bernoulli gating or rate-independence.)\n\n**F7 — LOW — \"vertical\" slam is injected into ECEF axis 0 (X), not local vertical** (`lib.rs:245`), so the \"vertical acceleration\" label is geometrically loose in a synthetic mission built off an ISS-derived local frame. Cosmetic given the [UNVERIFIED] framing.\n\n**F8 — LOW — 7 vs 20 kn comparison is not purely seed-controlled.** `highspeed.rs:221-223`: `phase` (an additive constant on every error sample) depends on `seed ^ speed_kn.to_bits()`, so each regime gets a different offset. Deterministic, but it means the same seed does not produce a matched noise realization across speeds. Minor because the whole error model is a formula anyway.\n\n**F9 — MEDIUM — report/STUDY overstate what was validated.** The central qualitative result (20 kn worse than 7 kn) is an assumption encoded in `speed_factor`/`q`, not a finding. Brief item (c) demanded process noise \"config-driven, no hardcoded magic\": the process-noise *multipliers* (`[1,1,1,1]` / `[6,4,1,2]`) are genuinely config-driven and documented as provisional pending U-H1/D43 (good) — but the error model that produces every headline number is a wall of hardcoded magic constants (`4.0, 0.75, 0.000_012, 0.025, 0.018, 75.0, 28.0`) with no lineage. STUDY.md does say \"reporting-harness outputs, not validated navigation predictions,\" which is honest, but the headline table's prominence outweighs the caveat.\n\n## What was done well (for the record)\n- `config_fingerprint` deliberately left untouched → default-mission `config_hash` stable.\n- RNG draw order preserved on the default path (wave `None` → no draws; `scaled_imu` `None` → same `rng.normal()` call count); default missions provably bit-identical.\n- [UNVERIFIED] labeling is present and honest throughout (wave model, process noise, synthetic accuracy, 30 h ceiling).\n- U-P1 dependency handled per the brief: `integration_notes` state graduated aging is absent, the 6 h binary gate would reject every case, and the 30 h ceiling is assumed; the 7 kn case's 30 h-exceedance is correctly reported as a **negative** margin (−8.57 h) rather than hidden. Endurance case coherent with the 30 h ceiling (6 h margin).\n- `validate_config` rejects >10.3 m/s and non-finite/degenerate turn/wave/IMU params.\n\n## D47 fix-round scenario specification (NOTE only, not fixed)\nFor the mandated **30 kn tier** and **100-km-after-good-fix-loss** scenario to be controlled and meaningful, they must (critically) be produced by *actually running mission + estimator*, else they inherit F1/F2's vacuity:\n- **30 kn tier:** raise the `validate_config` cap (currently 10.3 m/s) — 30 kn = 15.43 m/s is presently rejected — and confirm D46/D47 authorize >20 kn; add a distinct config-driven process-noise regime and distinct wave/slam parameters (documented provisional, [UNVERIFIED]); recompute convergence *distance* at 30 kn (a 20-min DR bridge ≈ 18.5 km) as a measured quantity, not `time×speed`.\n- **100 km after good-fix loss (D45 core):** run aided until the EKF covariance reaches steady state, record position error *and* covariance at the loss instant as the DR initial condition (start from a known-good converged state, not idealized zero); ephemeris cached at t=0, GNSS lost at that converged instant, then DR ≥100 km. Report position-error class at the 100 km mark and the ephemeris-age margin there against the graduated-aging inflation curve. Hold **distance** (100 km) constant across the 7/20/30 kn tiers on a shared seed (100 km ≈ 7–9 h displacement / ~2.9 h at 20 kn / ~1.9 h at 30 kn — so the ephemeris-age stress differs sharply by tier, which is the point). Wave model ON, and IMU must be truth-consistent (fix F5) so the measured DR growth reflects real aiding-loss inertial drift, not a rectified-disturbance artifact. Must exercise U-P1 graduated aging; if U-P1 is still unmerged, block-and-note rather than fabricate.\n\n---\n\n**FAIL** — gate is green and the work is honestly labeled, but the primary deliverable (the D46 study) does not run the system it validates: every headline number is a closed-form formula with the mission generator, wave model, speed-scaled IMU, and EKF entirely bypassed (F1); convergence is asserted, not measured, over a fictional multi-turn route (F2); there is a real backward-compat regression for <10 s missions that the determinism test cannot catch and the report implies is proven (F3); the wave model is non-zero-mean with a latent IMU/truth inconsistency (F4/F5); and the speed-scaled-IMU feature is untested (F6)."}],"stop_reason":"end_turn","stop_sequence":null,"stop_details":null,"usage":{"input_tokens":2,"cache_creation_input_tokens":1697,"cache_read_input_tokens":71461,"output_tokens":7548,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":0,"ephemeral_5m_input_tokens":1697},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":7548,"cache_read_input_tokens":71461,"cache_creation_input_tokens":1697,"cache_creation":{"ephemeral_5m_input_tokens":1697,"ephemeral_1h_input_tokens":0},"type":"message"}],"speed":"standard"},"diagnostics":null},"requestId":"req_011CdK8SqHe4ZadFhtmcC1SH","attributionAgent":"general-purpose","type":"assistant","uuid":"1baa37b3-25d1-4781-abe6-f9e997b8abb4","timestamp":"2026-07-23T14:16:37.500Z","effort":"high","userType":"external","entrypoint":"cli","cwd":"/home/od/work/leo-pnt","sessionId":"2313b09e-f4e0-4a4c-803f-9301c33ad4d4","version":"2.1.217","gitBranch":"HEAD","slug":"elegant-brewing-moler"}
