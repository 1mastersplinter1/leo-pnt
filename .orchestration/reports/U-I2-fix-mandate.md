# U-I2.1 fix mandate (Fable, per dual FAIL)
Read .orchestration/reports/U-I2-review-opus.md first; Sonnet-seat findings included below. Ordered fixes:
1. (Opus F1 + F4) Doppler Jacobian test integrity: add an FD test of the executive's geometric H construction (position + velocity + clock columns) against central differences over the REAL process_doppler linearisation path — either by exposing the linearisation for test or moving it into pnt-predictor per ARCHITECTURE module 7 (state which and why in the report). Rework the e2e test: measurement OFFSET from prediction (nonzero innovation) with a bounded, hand-derivable state-correction assertion so a wrong Jacobian fails.
2. (Opus F5 = Sonnet F-1) Elevation mask: production default must be a REAL mask (e.g. 5 degrees converted to radians, value [UNVERIFIED] pending link-budget work), API takes Option<f64> or a degrees-safe constructor to kill the -90.0-rad sentinel trap; executive-level test: below-mask observation → journaled reject. Disclose the default in the report + CONTRACTS v4 amendment (append a v4.1 note, do not rewrite v4).
3. (Sonnet F-2) Chi-square reject coverage: executive-level test with a divergent correlation_peak_hz → gate reject → integrity event journaled, zero filter updates.
4. (Opus F2) Ingress rejects journal the FULL measurement envelope (write_measurement) in addition to the integrity event; tests assert measurement_records grows on Orbcomm/OneWeb-disabled/off-GNSS rejects.
5. (Opus F3 = Sonnet F-4) epoch_json finite guard: non-finite value → no NDJSON line + journaled integrity event; test with poisoned covariance/state.
6. (Sonnet F-3) CONTRACTS v4.1: pin source_id = decimal NORAD catalog ID string for TrackerDoppler + required RFC3339 utc field; also fix the 'materialised at construction' wording (accuracies are accessor-derived at emission).
7. (Sonnet weak-test note) OneWeb enabled-branch test must attach a DopplerPipeline and assert the observation actually reaches the tracker route beyond routes_for (fused or gate-rejected — either proves pipeline reachability; assert journal/filter state accordingly).
Gate: PATH="$HOME/.cargo/bin:$PATH" cargo test && cargo clippy --all-targets -- -D warnings && cargo fmt --all -- --check. TDD, tests-first commit.
