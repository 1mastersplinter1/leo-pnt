# U-M1.1 fix mandate (Fable, per dual FAIL)
Both round-1 reviews are binding. Sonnet seat: F1 yaw sentinel (major), F2 schema-required-field tests, F3 clock-domain doc, F4 duration claim, F5 endpoint-only check. Opus seat: F1 yaw (same), F2 HDOP root cause, F3 D17a absent, F4 README false spec claim, F5 wrong test oracle, F6 position-prose overclaim, F7 checksum caveat, F8 time_week.

Ordered fixes:
1. YAW: encode_yaw(None) and expired-fill must emit 0 (spec: GPS_INPUT.yaw 0 = not available, 36000 = north; 65535 belongs to GPS_RAW_INT). Fix code, BOTH tests (correct oracle), README (remove the false "extension-field" justification, cite common.xml GPS_INPUT semantics), and report.
2. HDOP: stop ignoring HDOP/VDOP. Send a real nominal HDOP derived from horiz_accuracy (document mapping) so AP_GPS_MAV writes state.hdop and EK3_GPS_CHECK can pass; do NOT weaken EK3_GPS_CHECK in params (fix the bridge, not the check). Add a test asserting hdop is sent and finite.
3. Re-run the SITL acceptance (tools/sitl/run.sh + run_acceptance.py). Expected: EKF leaves constant-position mode, pos_horiz_abs set, ACCEPTANCE record written. If it still fails, diagnose from the pinned source as Opus did and report the exact gate — no "open uncertainty" without a source-level chase.
4. D17a characterisation (MANDATORY, gates sea trials): with the EKF aided and vehicle in a guided/auto steering state, stop the bridge stream and record from MAVLink telemetry: what ArduPilot Rover does at what timing (failsafe action, mode change, actuation state) on GPS_INPUT silence; repeat for the Case-A handoff candidates if cheap (mode-change command from companion). Write findings to tools/sitl/evidence/ + a "D17a characterisation" section in the report with VERIFIED excerpts. If a specific sub-experiment cannot run, say exactly why and mark [UNVERIFIED].
5. Smaller: schema-required tests for speed_accuracy_mps/vert_accuracy_m/msl_alt_m (Sonnet F2); README note on the same-host monotonic clock-domain assumption (Sonnet F3); fix duration-claim consistency (Sonnet F4); report prose — 0.08 m is driver-level ingestion evidence, not fusion evidence (Opus F6); checksum caveat wording (Opus F7); time_week decision: either send a real GPS week/ms derived from UTC or record the [UNVERIFIED] lag consequence explicitly (Opus F8); state whether the acceptance position check becomes continuous-tracking once aided (Sonnet F5) — if endpoint-only is kept, say so in README.
Gate: tools/.venv/bin/pytest -q tools/mavlink_bridge && tools/.venv/bin/ruff check tools/mavlink_bridge tools/sitl, plus the SITL acceptance run evidence. TDD: fix the test oracles first (red), then code (green).
