# Adversarial review — U-ST1.2 endurance "settle the cause" (commit 388ad4c)

## Mechanical gates — ALL PASS
- `cargo fmt --all --check` clean; `cargo clippy --all-targets -D warnings` clean.
- Tests: endurance module 7/7, `pnt-studies` lib 22/22, `pnt-estimator` 13/13, predictor Jacobian test passes.
- **Determinism**: regenerated `results.json` + `STUDY.md` via `cargo run --release --bin endurance-study` — **byte-identical** to committed (schema 3).
- **No trailers** on 388ad4c. **Scope clean**: diff is `endurance.rs` + `docs/studies/endurance/**` + report only; no estimator/mission/multisat edits.
- **Worktree left clean** (temporary instrumentation reverted via `git checkout`; `git status` empty, HEAD 388ad4c).

## (a) Bias-zeroed control — REAL and SOUND *as far as it goes* [confirmed]
The `sv_bias_hz=0` path is genuinely wired: `simulate(..., bias_enabled)` (`endurance.rs:622`) toggles `sv_bias_hz(id,seed)` vs `0.0` inside the truth generator's `predict()` call (`endurance.rs:698-702`); everything else is identical. `bias_zeroed_control_is_a_real_perturbation` guards it's a genuine counterfactual (injected bias materially non-zero — I confirmed live: `sv_bias(FIRST_ID)=0.95` for seed 3776782374 — and both arms sample the same epochs). Ratio 1.00 is real: the per-SV nuisance state (variance 100 m²/s², ~10 m/s σ; `fusion-executive/src/lib.rs:319` `satellite_bias_variance_mps2: 100.0`) absorbs a constant ~1 Hz transmit bias. **This legitimately refutes the "injected-bias-VALUE drives km-scale" hypothesis.** The prior handover-bias diagnosis is correctly retracted.

## (b) Grid reverted to 960 — CONFIRMED [PASS]
`SHELL_SLOTS 16→20` (`endurance.rs:66`) makes the orbital-element formulas byte-for-byte equivalent to `multisat::synthetic_fixture`: 16 planes × 20 slots × 3 shells, `raan = plane·360/16`, `anomaly = slot·18 + (plane%2)·9` (`endurance.rs:1192-1194` vs `multisat.rs:583-584`), identical inclinations/mean-motions. False "equatorial coverage gap" justification dropped, replaced with the measured "≥22 visible." `fixture_is_leo_not_meo` intact, checks all 960 SVs (verified passing). LEO guard and `truth_stays_at_sea_level_and_coverage_is_continuous` both green.

## (c)/(d) THE CRUX — the study lands on the WRONG cause; it over-claims a fundamental limit [CRITICAL / high confidence]

**The measurement path for position-from-Doppler EXISTS and is correct.** `geometric_range_rate_linearisation` populates the position partials `∂ṙ/∂p = -(v_rel − los·ṙ)/range` (`pnt-predictor/src/lib.rs:120-123`), numerically verified against finite differences for columns 0-2 (`pnt-predictor/tests/predictor.rs:6-46`). `update_doppler` copies these into H rows 0-2 (`pnt-estimator/src/lib.rs:201-204`), so the Kalman gain feeds Doppler innovation back into position. The EKF is **structurally capable** of observing position from Doppler-curve evolution.

**I instrumented the REAL `Executive`+`FilterStub` position covariance over a 60-min leg** (representative study seed 3776782374; temporary `#[ignore]` test, since reverted). Findings:

- Position covariance **converges and stays BOUNDED**: σ oscillates ~50–160 m across the whole leg, actively **decreasing at 242 epoch-substeps** (Doppler updates pull it down; propagation grows it back). It does **NOT** random-walk to km-scale. Example: t=600 s σ=159 m → t=900 s σ=66 m (a pure random walk cannot decrease — position is genuinely observed).
- Meanwhile the **actual horizontal error** reaches 611 m at the endpoint for this seed (and km-scale for the study's tail seeds) while the filter's own σ is only ~86 m — a **7–70× overconfidence**.

**This refutes both clauses of the study's headline cause.** If km-scale were a *fundamental observability floor*, the covariance would be km-scale too (the filter would *know* it is uncertain). Instead the filter is confident (~100 m) while being wrong (600 m–km): a **filter INCONSISTENCY / overconfidence**, i.e. by definition an **estimation problem** — driven by the clock/per-SV-bias null-space and linearization, not by an absence of position information. Position **is** observable (weakly, ~100 m σ); the residual km-scale error is exactly the "estimation bug" class the study says it is NOT.

- **Over-claim location**: `STUDY.md:59` / `U-ST1.2.md:64-65,85` — "the limiter is … **FUNDAMENTAL** weak Doppler-only position observability … **This is a fundamental observability limit, not an estimation bug**"; generated by `bias_control_verdict` (`endurance.rs:884`). Per the review mandate's (d): *"If the study over-generalizes a stack limitation into a fundamental LEO-physics claim, that is a FAIL."* It does exactly this.

- **Why the control cannot support the claim** [CRITICAL]: the bias-zeroed experiment holds the **identical nuisance-augmentation architecture in BOTH arms** — a fresh variance-100 bias per handover, and `retire_satellite_bias` (`pnt-estimator/src/lib.rs:177`) is **never called in the pipeline** (only in unit tests). Zeroing the injected *value* while keeping the estimator DOF cannot distinguish "fundamental physics" from "the estimator's own nuisance/clock null-space suppressing position and de-tuning the covariance." The decisive lever (bias continuity across handover / covariance-consistency correction / Q-retuning) lives in the estimator the study explicitly declined to touch ("config-level `endurance.rs` only"). A study cannot conclude "not an estimation bug" from an experiment that leaves the suspected estimation mechanism untested in both arms.

- **How to verify**: (1) run the `#[ignore]` covariance-trajectory instrumentation I used — position σ stays 50–160 m while error → 600 m+, i.e. overconfident/inconsistent. (2) The predictor FD test proves the position Jacobian is present and correct. (3) The 86 m RMS at 10 min already sits inside the handoff's own "~100–200 m satellite-only by accumulation" band (`HANDOFF_PROMPT_BLADERF.md:90-91`) — the mechanism demonstrably works; the *divergence* thereafter is a consistency failure, not a physics floor.

## (e) Honesty guardrails [mostly good — the failure is over-claim, not dishonesty]
Gate on (`Some(9.0)`), real EKF vs truth, no clamp/formula/target-fit, reports bad news, retracts its own prior diagnosis, byte-deterministic, scope config-only, no trailers. **RMS-metric change is legitimate** — my per-epoch trace confirms the endpoint is a spiky single-epoch excursion (mid-leg ~15–140 m, endpoint jumps to 600 m) while RMS-over-leg is stable; this is a sound metric correction, not target-fitting. No cherry-picking. The study is *honest*; its defect is a scientific over-reach on the cause.

## What WOULD be a PASS
The defensible, evidenced headline: "km-scale, 500 m not met over long legs, reproducible; **not** caused by the injected per-SV bias value; not fixed by leg duration or clock." That is honest and well-supported. The correctly-scoped cause is: **this synthetic EKF stack does not realize position-from-Doppler-evolution well enough to hold 500 m over long legs — position is weakly observable (~100 m σ) but the filter is inconsistent (true error ≫ covariance), an estimation/consistency limitation whose fix (bias continuity, retuning, consistency correction) is out of this study's self-imposed scope.** Instead the study asserts a *fundamental LEO-physics* limit ("not an estimation bug," "needs a different platform/aiding"), which the covariance evidence contradicts.

## Verdict
The two mandated mechanical fixes (960-grid revert, false-coverage-claim dropped, LEO guard intact) are done correctly, and the bias-zeroed control soundly kills the bias-*value* hypothesis. But the study then over-generalizes that single refutation into "fundamental observability limit, not an estimation bug" — the exact over-claim in the *other* direction that mandate (d) defines as a FAIL. Direct instrumentation of the real filter shows position covariance converges and stays bounded (~100 m) while true error runs 7–70× higher: position is observable; the km-scale error is filter inconsistency (an estimation problem). The decisive control holds the suspected estimator architecture fixed in both arms and therefore cannot support the conclusion drawn from it.

FAIL