(Reviewer: Opus scientific seat. Verdict: PASS with mandatory corrections before merge.)
Verified positive: byte-identical full reproduction (206.9s) across different thread counts; 3718 Hz/s Ku drift derivation re-derived correct; knee consistent with coherent-integration theory (30.1 dB-Hz floor + scalloping + margin); Fisher tail math recomputed exact (5.3008e-9), conservatism 1.4-2.4x direction correct; impairment spot-checks exact; quality-variance fit correctly judged non-invertible (structured residuals, saturation ~N).
Corrections required:
- A1 HIGH/high lib.rs:255-284,630-660 — two-signal false capture is a CONSTRUCTION ARTIFACT: both signals share identical PN code AND phase (reference() fixed seed 0x12345678, delay 37 for both); despread composite is a clean two-tone and the Kay discriminator reports its power-weighted centroid (locks at (f1+f2)/2, verified). With DISTINCT sequences (the realistic co-channel case incl. Starlink channels) behavior is CORRECT (locks own signal or NoDetection). Fix: harness injects interferers with distinct PN seeds; rerun the two-signal study; rewrite the headline as artifact-with-diagnosis; ADD the one genuine same-code analog — specular multipath: same code, DIFFERENT delay — as a new measured case.
- B1 MED/high lib.rs:26 — knee measured only at a favorable 8.5 Hz sub-bin offset (-1.0 dB scalloping); half-bin worst case is -3.9 dB and shifts the knee ~2-3 dB (Pdet at 34 dB-Hz: 0.988 -> 0.232). Add a sub-bin-position sweep or at minimum the disclosed 32-35 dB-Hz knee range.
- B3 LOW — add binomial 95% CIs to Pdet (n=500 -> +/-4.4% at the knee).
- C1 LOW — note consecutive-seed first-draw correlation (0.194 lag-1, confined to draw 0, block statistic 0.0025) as a harness note; splitmix64 hash optional hardening.
- Minor: "three orders short" -> ~2.8 orders (need ~5.7e8 blocks).
